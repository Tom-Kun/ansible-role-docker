[Service]
Environment="HTTP_PROXY={{ docker_proxy_http_proxy }}"
Environment="HTTPS_PROXY={{ docker_proxy_https_proxy | default(docker_proxy_http_proxy) }}"
Environment="NO_PROXY=127.0.0.1,localhost{% if docker_proxy_no_proxy is defined %}{{ docker_proxy_no_proxy | join(',') }}{% endif %}
